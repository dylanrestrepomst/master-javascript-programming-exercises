function getLengthOfWord(word) {
  // your code here
  return  `tiene ${word.length}  palabras`; // creamos una funcion en el que colocamos la ${palbra que colcamos en el console log}
  // ponemos tiene y palabras dentro del string usamos . length para que nos diga el numero de palabras tambien se usa para ver el tamaño del array
}
console.log(getLengthOfWord("brandy"));
